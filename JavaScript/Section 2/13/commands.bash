# Install vitest as a development dependency
npm install --save-dev vitest

# set script to run vitest in package.json
# "scripts": {
#     "test": "vitest --globals"
# }
# --globals flag is used to make sure that the global variables test environment such as "it", "describe", "expect" are available in the test files.
